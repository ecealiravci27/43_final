package Model.Fields;

//the chance fields on the board inherit from SuperField
public class ChanceField extends SuperField{
    public ChanceField(String name, String description, int ID) {
        super(name, description, ID);
    }
}
